fn main(args) {
  var input = fs.get_lines("./data");

  var prev = -1;
  var incs = 0;

  for (var i = 0; i < #input - 3; i++) {
    var sum = $input[i]
            + $input[i + 1]
            + $input[i + 2];

    if (i && sum > prev)
      incs++;

    prev = sum;
  }

  io.println(incs);
}
